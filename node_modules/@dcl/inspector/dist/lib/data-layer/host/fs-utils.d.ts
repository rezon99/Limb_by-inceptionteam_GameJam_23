import { FileSystemInterface } from '../types';
export declare function getFilesInDirectory(fs: FileSystemInterface, dirPath: string, files: string[], recursive?: boolean, ignore?: string[]): Promise<string[]>;
export declare const DIRECTORY: {
    ASSETS: string;
    SCENE: string;
};
export declare const EXTENSIONS: string[];
export declare function withAssetDir(filePath?: string): string;
export declare function getFileName(fileName: string, ext: string): string;
export declare function getCurrentCompositePath(): string;
