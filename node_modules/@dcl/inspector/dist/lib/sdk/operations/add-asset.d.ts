import { Entity, IEngine, Vector3Type } from '@dcl/ecs';
export declare function addAsset(engine: IEngine): (parent: Entity, src: string, name: string, position: Vector3Type) => Entity;
export default addAsset;
